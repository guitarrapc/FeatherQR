# Image decode accuracy, measured against other readers

## Purpose

The clean-image work of the 2.0.0 plan (F13 to F18) was driven by this library's own renders of this library's own symbols: a sweep found a failing class, the class got a letter, the letter got a fix. That found real defects and it has no end, because the next sweep always finds another class, and it leaves one question open: whether the decoder got better or the tests got easier to pass.

This plan replaces that loop with an outside yardstick. The target is the set of images **another reader decodes and this library does not**, counted render for render on the same image, over symbols from every encoder the repository can run. The count is called the gap. It cannot be moved by changing a test, it says where the decoder is behind rather than where it is merely imperfect, and it has an end: zero, or a residual that is explained.

A sweep on `main` (2026-09-21) showed the yardstick is usable and where the gap is.

- **The encoder does not matter.** Seven Standard QR encoders (this library, ZXing.Net, QRCoder, QrCodeGenerator, CodeGlyphX, libzint, qrtool), three for Micro QR and rMQR. Same payload and same render parameters per case. Per render kind the read rate differs between encoders by at most 2.5 points (10 of 400), which is mask choice: 40 to 50 % of the QrCodeGenerator, libzint and qrtool matrices differ from this library's, 6 % of ZXing.Net's, 1.5 % of QRCoder's and CodeGlyphX's. Every library's own image writer reads 100 %. 135,000 decodes, no misread.
- **The gap is Standard QR.** zxing-cpp reads 2,002 Standard QR renders this library does not, 1,907 of them in five kinds. For Micro QR and rMQR it is the other way round by a wide margin (rMQR at any rotation: 5 to 16 % against 100 %), so those two have no outside yardstick and are tracked by their absolute rate.
- **ZXing.Net's reader is behind this library in every kind** and is not a target. It stays in the table as a second reference.

## Baseline

The library as of `main` at 416a3b1, which reads as a874f5e did (the first baseline, taken there, reproduced image for image). 400 Standard QR cases (ten per version, random level, numeric / alphanumeric / URL-like byte payload, longer than the version below holds so that the version is the one asked for), 400 Micro QR (M1-M4, the version pinned), 640 rMQR (twenty per version, pinned); every case through every encoder and every kind. Density in px/module. "Gap" is renders zxing-cpp reads and this library does not; "reverse" is the opposite. qrtool's R17x43 symbols are excluded (see Verification notes). The Standard QR table was taken again after the PR review of phase 1 found the first payload rule putting 230 of 400 cases under their version (Progress log); Micro QR and rMQR did not move by a row.

### Standard QR (2,800 renders per kind, 2,000 for the last)

| Kind | FeatherQR | zxing-cpp | ZXing.Net | Gap | Reverse |
|---|---|---|---|---|---|
| Crisp nearest-neighbour, random offset, 1.0-1.25 | 2,800 | 654 | 130 | 0 | 2,146 |
| Crisp, 1.25-1.5 | 2,800 | 2,424 | 886 | 0 | 376 |
| Crisp, 1.5-2 | 2,800 | 2,765 | 2,213 | 0 | 35 |
| Crisp, 2-3 | 2,800 | 2,784 | 2,549 | 0 | 16 |
| Crisp, 3-6 | 2,800 | 2,800 | 2,737 | 0 | 0 |
| Anti-aliased path, 1.0-1.25 | 41 | 49 | 7 | 39 | 31 |
| **Anti-aliased path, 1.25-1.5** | 1,487 | 1,926 | 516 | **688** | 249 |
| **Anti-aliased path, 1.5-2** | 2,637 | 2,718 | 1,149 | **149** | 68 |
| Anti-aliased path, 2-3 | 2,800 | 2,799 | 1,949 | 0 | 1 |
| Anti-aliased path, 3-6 | 2,800 | 2,800 | 2,598 | 0 | 0 |
| **Bilinear upscale of a 1 px/module image, 1.5-2** | 2,022 | 2,583 | 641 | **644** | 83 |
| Bilinear upscale, 2-2.5 | 2,775 | 2,791 | 1,536 | 25 | 9 |
| Bilinear upscale, 2.5-3.5 | 2,797 | 2,783 | 1,922 | 3 | 17 |
| Downscale of an 8 px/module image, 1.5-2.5 | 2,789 | 2,793 | 1,678 | 11 | 7 |
| Downscale, 2.5-4 | 2,800 | 2,800 | 2,333 | 0 | 0 |
| Right-angle turn, crisp, 1.5-3 | 2,800 | 2,800 | 2,395 | 0 | 0 |
| Mirrored, crisp, 1.5-3 | 2,800 | 2,793 | 2,401 | 0 | 7 |
| **Supersampled, any rotation, 2-3** | 2,735 | 2,746 | 1,336 | **65** | 54 |
| Supersampled, any rotation, 3-6 | 2,794 | 2,800 | 2,243 | 6 | 0 |
| Supersampled, any rotation, keystone up to 6 %, 2-6 | 2,789 | 2,800 | 1,712 | 11 | 0 |
| **Supersampled, any rotation, keystone 6-20 %, 3-6** | 2,439 | 2,800 | 1,009 | **361** | 0 |
| JPEG quality 50-90, crisp, 2-4 | 2,800 | 2,793 | 2,520 | 0 | 7 |
| The encoder library's own image writer | 2,000 | 1,995 | 1,631 | 0 | 5 |

Where the five gap kinds fail. Below 2 px/module the status is `DataUncorrectable` almost throughout (bilinear 1.5-2: 771 of 778; anti-aliased 1.25-1.5: 1,308 of 1,313): the finders are found and the sampling is lost, which is what F18(a) left. Keystone 6-20 % splits by version: v10-20 reads 764 of 771, v1-9 574 of 629, v21-30 582 of 699, v31-40 519 of 701, and density barely matters (3-4: 811 of 924, 5-6: 812 of 945).

### Micro QR (1,200 per kind) and rMQR (1,899 per kind)

| Kind | Micro QR | zxing-cpp | Gap | rMQR | zxing-cpp | Gap |
|---|---|---|---|---|---|---|
| Crisp, 1.0-1.25 | 1,197 | 201 | 0 | 1,899 | 88 | 0 |
| Crisp, 1.25-6 (four bands) | 1,200 each | 573-1,197 | 0 | 1,893-1,899 | 309-1,674 | 0 |
| **Anti-aliased path, 1.0-1.25** | **249** | 45 | 3 | **334** | 15 | 0 |
| **Anti-aliased path, 1.25-1.5** | **1,048** | 200 | 3 | **1,489** | 51 | 3 |
| Anti-aliased path, 1.5-2 | 1,185 | 491 | 3 | 1,866 | 128 | 0 |
| Anti-aliased path, 2-6 (two bands) | 1,200 each | 1,133-1,169 | 0 | 1,899 each | 626-969 | 0 |
| **Bilinear upscale, 1.5-2** | **1,050** | 162 | 3 | **1,643** | 9 | 3 |
| **Bilinear upscale, 2-2.5** | **1,056** | 532 | **54** | 1,854 | 94 | 6 |
| Bilinear upscale, 2.5-3.5 | 1,191 | 718 | 3 | 1,899 | 35 | 0 |
| Downscale, 1.5-2.5 | 1,191 | 814 | 0 | 1,896 | 384 | 0 |
| Downscale, 2.5-4 | 1,200 | 1,170 | 0 | 1,899 | 774 | 0 |
| Right-angle turn / mirrored | 1,200 each | 1,012 / 983 | 0 | 1,899 each | 959 / 0 | 0 |
| Supersampled, any rotation, 2-6, keystone up to 6 % (three kinds) | 1,200 each | 789-1,164 | 0 | 1,899 each | 86-306 | 0 |
| **Supersampled, any rotation, keystone 6-20 %, 3-6** | 1,200 | 1,143 | 0 | **1,562** | 187 | 15 |
| JPEG / own image writer | 1,200 each | 1,164 / 1,179 | 0 | 1,899 each | 1,402 / 1,677 | 0 |

Micro QR's bilinear 2-2.5 cell is the only one where the finder is the loss (`NotDetected` 111 of 144). rMQR's keystone failures grow with width: 139 modules reads 95 of 240, 99 reads 182 of 240, 27 and 43 read everything, which matches the F18(e) measurement (the perspective search's resolution over a long symbol, not the row pitch).

### Real images (phase 2, `main` at 02168b0)

zxing-cpp's black-box sample sets at commit 7e51e7b, 156 images, each read upright and at the three other right angles (624 reads). "Content" is images this library got through and then decoded into another text or refused in the bit stream; it is not gap.

| Set | Images | FeatherQR | zxing-cpp | ZXing.Net | Gap | Reverse | Content |
|---|---|---|---|---|---|---|---|
| `qrcode-1` | 8 | 24/32 | 32 | 24 | 8 | 0 | 0 |
| `qrcode-2` | 56 | 104/224 | 201 | 100 | 56 | 3 | 52 |
| `qrcode-3` | 18 | 44/72 | 72 | 56 | 28 | 0 | 0 |
| `qrcode-4` | 24 | 36/96 | 60 | 64 | 24 | 0 | 0 |
| `qrcode-5` | 16 | 29/64 | 64 | 64 | 35 | 0 | 0 |
| `qrcode-6` | 15 | 58/60 | 60 | 60 | 2 | 0 | 0 |
| **Standard QR** | 137 | **295/548** | 489 | 368 | **153** | 3 | 52 |
| `microqrcode-1` | 16 | 60/64 | 59 | | 3 | 4 | 0 |
| `rmqrcode-1` | 3 | 12/12 | 12 | | 0 | 0 | 0 |

On photographs this library reads 54 % of what is there and zxing-cpp 89 %; ZXing.Net, behind in every synthetic kind, is ahead here (67 %). The gap is 28 % of the Standard QR reads, against 3.1 % in the synthetic sweep, and it is of another kind:

- **`qrcode-5` and `qrcode-1` are the threshold.** Every failure in `qrcode-5` is `NotDetected` (35 of 35) on photographs with a shading gradient across the symbol, where one global threshold puts a whole side of the symbol in one class. A diagnostic run that binarized each image against its local mean before the decoder saw it (not a candidate, a probe: window a tenth of the image, 8 % under the mean) read 63 of 64 there and 32 of 32 in `qrcode-1`, 42 of the 43 gap reads of the two sets. Over the whole corpus it moved Standard QR 295 → 335 (+54, −14) and Micro QR 60 → 63; the 14 it lost say it cannot simply replace the global threshold.
- **`qrcode-3` and `qrcode-4` are not the threshold** (the probe moved them 44 → 44 and 36 → 37). They are larger versions at 2.5 to 3 px/module, turned and keystoned, some already binarized by the camera, and blurred photographs: the same classes as the synthetic rotation, keystone and low-density kinds, failing as `DataUncorrectable` with the finders found.
- **`qrcode-2` is a mixed bag** and is triaged by image: an inverted symbol, a Model 1 symbol, round modules, tilted low-resolution captures, and the eleven Shift_JIS images counted as content.

Content, 52 reads of 13 images, all in `qrcode-2`: eleven Byte-mode symbols that carry Shift_JIS with no ECI header, which zxing-cpp reads by guessing the character set and this library decodes as ISO-8859-1, the specified default, once the bytes fail as UTF-8 (another text, by design); one GS1 symbol (`UnsupportedContent`); one symbol whose bit stream is refused and that no reader reads. None of it is image decoding, and it stays out of this plan.

## Scope

| In | Out |
|---|---|
| The gap to zxing-cpp on Standard QR, kind by kind, largest first | ZXing.Net's reader as a target: it is behind in every kind |
| Absolute read rates for Micro QR and rMQR where they are under 99 %: anti-aliased input under 1.5 px/module, bilinear upscales up to 2.5, rMQR keystone past 6 % on wide symbols | Beating zxing-cpp where neither reads (Standard QR anti-aliased at 1.0-1.25: 52 and 58 of 2,800). A module centre covers under half of any pixel there; that needs its own decision, as F18(c) did |
| The sweep as a repository tool, deterministic, so a gap number can be reproduced by anyone | Encoding conventions between libraries (ECI, UTF-8 defaults, Kanji). The payloads are ASCII so that a failure is an image failure |
| A corpus of real images with known text, read by all three readers | The speed of the image path, except the Micro QR and rMQR failure path that phase 3 multiplied (phase 3b). The image-path speed work's rule that results stay bit-identical does not bind the accuracy phases: a phase here that moves the threshold or the sampled grid says so and re-baselines. Phase 3b is held to it |
| The decoder specs' envelope tables and the fixtures spec's oracle notes | F19 (the cross that becomes a finder candidate below 2.25 px/module). It stays in the 2.0.0 plan; no phase here may make it worse |
| | Public API. Nothing is added or changed |

## What has to stay true

- **No misread.** A decode that returns text returns the encoded text, over every sweep and the corpus. The baseline is zero.
- **Nothing is lost silently.** Every phase compares render for render against the commit before it and lists what stopped reading, per kind. "Reverse" does not shrink without a line saying why.
- **Failure paths are measured on failures.** Each phase reports two-level noise, blurred noise, a damaged symbol of each symbology, and another symbology's image, fastest of several interleaved runs, beside the success-path benchmarks. A read that costs every unreadable image a multiple is a trade to be stated, not a side effect.
- **The finder's candidate list on noise is pinned.** Candidate counts on the two-level noise set stay identical unless the phase is about the finder, and then the change is the phase's headline number.
- No allocation on the decode path, no `unsafe`, no public surface.

## Approach

Diagnose before repairing, with a column the earlier rounds lacked until F18(e): sample each failing render through the **true** transform, then matrix-decode. A render that reads that way failed in the frame (finder centres, dimension, perspective); one that does not failed in the pixels (threshold, where a module is sampled). The split decides which candidates are worth building, and it is cheap because the sweep knows every render's geometry.

Candidates, by the kind they are expected to move. None is chosen yet; each gets one hypothesis, one measurement, and a recorded verdict.

0. **The threshold under uneven lighting** (real images: `qrcode-5`, `qrcode-1`, part of `qrcode-2`). A second binarization tried when the global one finds no symbol, so that an evenly lit image pays nothing: a threshold per block from the block's own range with flat blocks taking their neighbours' (what ZXing's hybrid binarizer and zxing-cpp's local-average one do), or the global method run per tile and interpolated. What it has to be measured against is this plan's own history: every loosening of what reaches the finder has paid in candidates on noise, the finder's grey-level second look needs the dark and light levels the global histogram gives, and the image-path speed plan holds the binarizer bit-identical, so a local pass is an added path and not a change to the first one.
1. **Pixels below 2 px/module** (anti-aliased 1.25-2, bilinear 1.5-2.5: a gap of about 1,500, and the Micro QR / rMQR cells beside them). The threshold sits near 144 because edge greys fall in the dark class; the midpoint of the two class levels gave bilinear upscales +21 to +54 of 300 in the F18(a) prototype and nothing on anti-aliased edges. Reading a module as the mean over its footprint instead of one pixel. A threshold local to the symbol once the finders are known. A 2x resample of the symbol's own area, which the F18(a) round measured as the best reader and rejected only because it ran on the whole image of every failure.
2. **Keystone 6-20 %** (gap 361, zxing-cpp 100 %). The version split says the frame: v1 has no alignment pattern and v2-6 one, and from v21 up there are many and the decoder anchors on one. A mesh over several alignment patterns; corners from the finders' outer edges and the timing lines where there is no pattern to anchor on.
3. **Rotation at 2-3 px/module** (gap 65, reverse 54) and the small cells (bilinear 2-2.5, downscale 1.5-2.5). Diagnosed after 1, which probably moves them.
4. **Micro QR bilinear 2-2.5** (`NotDetected`): the single finder under blur. **rMQR keystone on wide symbols**: the perspective search's step over 99 and 139 modules.

## Phases

Each phase follows the test-first workflow, updates the decoder specs in the same change, and appends a Progress log entry with Done / Lessons / benchmark delta. Every phase reports the full baseline table again, not only the kind it aimed at.

| # | Priority | Phase | Contents | Exit |
|---|---|---|---|---|
| 1 | ~~**P0**~~ | ~~The sweep as a tool~~ **Done 2026-09-21** | The measurement moves from a throwaway probe into `tools/`, beside the interop fixtures: the encoders above, the test renderers, the three readers, fixed seeds, a gap table and a per-render CSV as output, and a mode that compares two trees render for render. QRCoder gets a pin with the other tool packages | The baseline above reproduces exactly from a clean checkout; two runs are identical |
| 2 | ~~**P0**~~ | ~~Real images~~ **Done 2026-09-21** | zxing-cpp's black-box sets for QR, Micro QR and rMQR (Apache-2.0), with their expected texts, read by all three readers; acquisition follows the fixtures spec's toolchain policy (committed if small, otherwise fetched by pinned commit and checksum). The gap table gets a corpus section | The corpus gap is a stated number per set. If it is larger than or different in kind from the synthetic gap, the phases below are reordered and the reason recorded |
| 3 | ~~**P0**~~ | ~~The threshold under uneven lighting~~ **Done 2026-09-23** | Added by phase 2. Approach 0: where in the decode a second binarization runs, which one, and what it costs the images that hold no symbol | `qrcode-5` and `qrcode-1` at gap 0 or each residual explained; no read lost in the corpus or in any synthetic kind, render for render; candidate counts on the two-level noise set unchanged on the first pass; the no-symbol benchmarks and the failure paths stated before and after |
| 3b | ~~**P0**~~ | ~~Micro QR and rMQR failure path, its own PR before phase 4~~ **Done 2026-09-24** | Speed only, every result bit-identical: the rMQR sub-finder template search with several positions scored at a time (92 % of a failing rMQR decode on noise); Micro QR codeword extraction and grid sampling (58 % of its failing decode between them); sweep candidates identical to ones the strided pass already tried, skipped (10-20 %). Not a lever: stopping the Micro QR and rMQR searches at a verdict on the content (an unmapped Kanji symbol costs 2-19 ms against 8-28 µs for a mappable one, as on `main`), because the search after it also looks for every other symbol in the image, and stopping it loses their reads (the sixth branch-review round below). Each lever measured on its own, one hypothesis a variant, with a parity test against the current form. Not the format gate or a timing check in front of the searches: both measured and refuted (Progress log, "Micro QR and rMQR failure path, investigated") | Sweep and corpus identical image for image; failure time on 512 × 512 noise-like images (1 px two-level noise, a textured ramp) stated before and after per symbology and per lever, with the kernel ratio beside the end-to-end delta; success-path benchmarks not worse; refuted variants recorded with their numbers |
| 3c | **P0** | Micro QR false results: the grid's structure checked before error correction, its own PR | Measure first: false reads and verdicts on Standard QR and rMQR images decoded as Micro QR (a sweep of at least 1,000,000, both polarities, shadows and ramps), and real Micro QR reads under the spec's lighting bounds. Then a check of the timing patterns and separator with an error budget before Reed-Solomon, and, if it holds, the per-level refusals of the regional pass removed (M3-M and M4-M at their correction limit, an empty read that needed correction, the global verdict at that limit trying the regional pass) and regional M1 reads recovered | No false read or verdict beyond `main` on the foreign sweep, `main`'s own M1 false reads fewer, no real read lost within the lighting bounds against this branch, and each rule's edge pinned by a test |
| 4 | **P1** | Pixels below 2 px/module | Diagnosis column first, then the candidates of Approach 1, Standard QR first and the shared pieces for all three | Standard QR gap under 1 % of renders in every kind from 1.5 px/module up, and no larger than the reverse at 1.25-1.5; Micro QR and rMQR at or above 99 % from 1.5 px/module up; losses listed; failure-path cost stated |
| 5 | **P1** | Standard QR under keystone | Diagnosis by version band, then Approach 2; `qrcode-3` and `qrcode-4` are this phase's and phase 4's real images, and each reports them | Gap under 1 % at keystone 6-20 %, the flat and up-to-6 % kinds identical or better render for render |
| 6 | P2 | The small Standard QR cells, and `qrcode-2` by image | Approach 3, only what phases 3 to 5 left; the `qrcode-2` gap triaged image by image into a class above, a new class, or out of the envelope (inverted, Model 1) with the reason | Each residual cell is under 0.5 % or carries a recorded cause |
| 7 | P2 | Micro QR and rMQR absolutes | Approach 4 | Micro QR bilinear 2-2.5 at or above 99 %; rMQR keystone 6-20 % at widths 99 and 139 measured against the true-transform column, and either repaired or recorded as the envelope with its number |
| 8 | P2 | Fold | Envelope tables, decisions, refuted candidates and lessons into the three decoder specs; the sweep and the corpus into the fixtures spec; this plan deleted | Nothing here is only here |

Phase 3b was added after phase 3: the regional attempt made every failing Micro QR and rMQR image with grey in it pay their failure path up to twice more (once a polarity), and that path is 70-120 ms on a noise-like 512 × 512 image. The multiple is largest where `main` fails cheaply: a ramp with ±20 noise and no symbol, which the global threshold splits into two solid halves, costs `main` 0.2 ms at 256 px and the regional pass turns the noise into finder-like specks, 16-41 ms for Micro QR and rMQR (80-200x; 104-163 ms against 6-29 ms at 512-640 px), while ±8 noise leaves every block flat and costs nothing more. Phase 3b measures its exit on this case as well as on the textured ramp. It is speed work with no read to gain, so it goes in its own PR, ahead of phase 4 so that the accuracy phases after it measure their own costs against a failure path that is no longer dominated by false candidates.

Phase 3c was added by the branch review of phase 3. Micro QR's matrix decode checks only the format information, to 3 bit errors of the nearest of its 32 words, which about 56 % of random 15-bit words pass (32 × 576 / 2^15), and no timing pattern or separator, so nearly every grid sampled on texture reaches Reed-Solomon; a random word that Reed-Solomon accepts is at the correction limit more than 99.8 % of the time at every level. The regional pass multiplies such grids, and the review kept finding false results at the limit on the level the last rule left out: after M3-M and M4-M were refused, about 4 in 1,000,000 foreign images read as M3-L and 1 as M2-M, and about 9 reported `UnmappedCharacter` from the global attempt where `main` reports no symbol, since this branch ranks a verdict above a correction failure. Refusing by error count cannot tell a real symbol at its limit from a false one, and loses real M4-M symbols at some placements. zxing-cpp has the same exposure with weaker checks (a local binarizer by default, two timing modules, no misdecode-protection margin) and CodeGlyphX checks finder, separator and timing modules against an error budget; neither refuses by error count. The expected gain of a structural check is an estimate until measured.

Phases 1 and 2 were first because they could redirect everything after them, and phase 2 did: the real-image gap is nine times the synthetic one in proportion, and the largest single cause in it, the global threshold under a lighting gradient, is a class no synthetic kind draws. It goes first as phase 3. The synthetic phases keep their order behind it, because the rest of the real-image gap (`qrcode-3`, `qrcode-4`) is the classes they already target, and from here on every phase reports the corpus beside the sweep.

## Verification notes

- A row's key is how its image was made, and beside it the row carries a digest of the pixels. `compare` keeps a pair whose digests differ out of gained and lost: if an encoder's mask choice or a renderer changes between two trees, the pair is two images, and the read that moved is not the decoder's.
- The sweep pairs by construction: a case fixes the payload, the level and every render parameter, and each encoder's symbol goes through the same parameters. Seeds are arithmetic on the case and kind indices, never `GetHashCode`, which is randomized per process.
- libzint's creator (the pinned ZXingCpp package) dies with an access violation on some payloads, some of the time (Standard QR case 301, about one run in three, alone in its process). The first reading, that readers on other threads caused it, was wrong. The sweep runs the creator in a worker process, retries the case it died on, and gives a case up only after eight deaths in a row. A drop is rare, not impossible, so it is never silent: the case gets a row saying so and the run exits with a failure, because its images are no longer the set other runs have. A process killed that way can linger and hold its files: build to another output directory rather than wait for it.
- qrtool 0.13.2 emits a wrong R17x43 symbol at level M: about 88 modules differ from this library's and libzint's matrices, which agree byte for byte, and zxing-cpp does not read it either; level H is only the documented tail defect. R17x43 from qrtool is excluded from every table; the fixtures spec carries the note.
- A synthetic kind cannot stand in for a photograph's lighting, so the threshold phase's regression renders need a generator of their own: a symbol under a luminance ramp and under a soft shadow edge, with the ramp's depth as the parameter, checked against the corpus sets before it is trusted as their stand-in.
- A regression test added by a phase is a render of the failing class with its geometry asserted (drawn corners), plus the negative that the repair must not admit, each failing with the repair removed. The sweep is the measurement, not the test.
- Mutation checks per phase: every new bound and gate must fail a test when moved; a guard no sweep and no test can tell from its absence is deleted.

## Progress log

### Phase 1: the sweep is a tool, and the baseline reproduces (2026-09-21)

**Done.** `tools/QRImageDecodeSweep`, in the solution beside the interop fixtures: `sweep` (seven encoders for Standard QR, three for Micro QR and rMQR, 23 kinds drawn by the test suite's own renderers linked from `tests/FeatherQR.Tests/Shared`, three readers), `corpus`, `compare` (two result files image for image: gained, lost, every lost image by name) and `import-corpus`. Output is a per-image CSV and the gap table as Markdown. Every package it needs was already pinned centrally. The fixtures spec has the section ("Image decode sweep") and the two oracle defects as lessons.

**Exit met.** Against the throwaway probe's result files, which were this plan's first baseline: 134,877 of 134,877 images identical in status, in all three readers' verdicts, and in density and size (63,600 Standard QR, 27,600 Micro QR, 43,677 rMQR). Three full runs wrote byte-identical files. The baseline was taken at a874f5e and the tool ran at 02168b0, so the decode-speed change between them (#415) moved no read.

**Lessons.** The libzint crash was misdiagnosed at first. It first appeared with readers running on other threads, a serial scan of the same cases passed, and "not thread-safe" fitted; a lock did not help, which should have ended that reading and instead got explained away as the readers' threads. Moved into a serial pass of its own, it crashed there, on the same case, one run in three. A fault that is probabilistic per input looks like a race under every test that varies the threading and nothing else; the test that separates them holds the threading still and repeats the input. And escapes do not survive this machine's shell tooling (a `\u0001` separator arrived in the source as a raw control character, and a quoted apostrophe ended a script): generated edits go through files, and a sort key is built from `ThenBy`, not from a joined string.

**Benchmark delta.** None; nothing under `src/` changed. The full sweep takes about two minutes on this machine and the corpus a few seconds.

### Phase 1, review round: the version each case asks for is the version it gets (2026-09-21)

**Done.** Four review comments on the tool's PR, three taken and one answered in the docs.

- **Taken: Standard QR cases did not land on their version.** The payload was 75-100 % of the asked version's capacity, and from version 6 up the version below holds more than 75 % of it, so 230 of 400 cases encoded smaller: version 40 had two cases and 39 three, where the plan said ten each. The per-band figures were never wrong (they read each symbol's own version), but the coverage was thin exactly where F13-F18 had found a class. The payload is now longer than the version below holds and no longer than the asked one does; every version has its ten, and three cases see an encoder choose a neighbouring version through its own segmentation. The Standard QR baseline was taken again: gap 1,870 → 2,002, the same five kinds, the encoder spread at most 10 of 400. Micro QR and rMQR pin the version in every encoder and did not move by a row.
- **Taken: a comparison paired by render parameters alone.** If an encoder's mask choice or a renderer changed between two trees, `compare` would have paired two different images and charged the difference to the decoder. Each row now carries a digest of its pixels, and a pair whose digests differ is counted in a column of its own and kept out of gained and lost.
- **Taken in part: a case dropped after eight creator deaths changes the row set.** Eight attempts make that rare and cannot make it impossible. The retries stay, because an unknown payload may need them, but a drop is no longer silent: the case gets a row saying so, the table lists it, and the run exits with a failure.
- **Answered: the supersampled renderer's four-module quiet zone round Micro QR and rMQR.** True, and left: those kinds measure what the rotation tests draw, the renderer is the tests', and a wider quiet zone is within both specifications. The spec and the kind's comment now say so.

**Lessons.** "Ten per version" was a claim about the generator that nothing checked, and a claim about a sample is cheap to check: count it. The sweep now counts the cases whose symbol is not the version asked for and prints the count, and it prints zero. A key that names how an image was made is not the image; the two agree until something upstream changes, which is the moment a comparison is needed most.

**Benchmark delta.** None; nothing under `src/` changed. Two full runs after the change are byte-identical.

### Phase 2: real images, and they reorder the plan (2026-09-21)

**Done.** zxing-cpp's black-box sets for the three symbologies (`qrcode-1` to `-6`, `microqrcode-1`, `rmqrcode-1`; 156 images, 1.1 MB, Apache-2.0) are committed under `tests/FeatherQR.Tests/Fixtures/RealImages/zxing-cpp-samples` with the LICENSE and a PROVENANCE.md naming commit 7e51e7b, the commit the Structured Append captures already came from. They are small enough to commit, so nothing is fetched at run time. No test reads them yet; the tool does, at the four right angles, as that repository's own runner does. The numbers are the "Real images" table under Baseline.

**Exit met, and its condition fired.** The corpus gap is stated per set: 153 of 548 Standard QR reads, 3 of 64 Micro QR, 0 of 12 rMQR. It is nine times the synthetic gap in proportion and its largest cause is of another kind, so the phases are reordered: the threshold under uneven lighting is the new phase 3, ahead of every synthetic cell. The evidence is a diagnostic, not a candidate: a local-mean binarization in front of the decoder reads 42 of the 43 gap reads of the two sets that fail as `NotDetected` under a lighting gradient, moves the two blurred, keystoned sets by a net of one read, and loses 14 reads elsewhere.

**Lessons.** "Misread" needed a second look before it meant anything: all 44 were one class, Byte-mode Shift_JIS with no ECI header, which this library decodes as the specified default on purpose and zxing-cpp reads by guessing. Counting them as gap would have put a character-set policy at the top of an image-accuracy plan, and counting them as reads would have hidden them, so they have a column of their own, together with the bit streams this library refuses. The synthetic sweep was built from the failure classes this project already knew, which is why it had no kind for the one that matters most on photographs; a corpus someone else collected is what finds the class nobody here thought to draw. And ZXing.Net, last in every synthetic kind, is ahead of this library on photographs: its hybrid binarizer is the difference, and that is one more pointer at phase 3.

**Benchmark delta.** None; nothing under `src/` changed.


### Phase 3: a regional binarization after both polarities, and what it costs (2026-09-23)

**Done.** `LocalBinarizer` in `Internals/ImageDecoders`, shared by the three image decoders: each 8 × 8 block gets a black point from its mean (half its minimum when flat, or the 1:2:1 mean of its top, left and top-left neighbours' when that is above it), and each pixel is read against the mean black point of the 5 × 5 blocks around its block. That is ZXing's hybrid scheme, which zxing-cpp also uses. Each decoder tries it once, after both global polarities have failed, into the buffer the inverted retry already rented, and reports the first attempt's status when it fails too. It is skipped when it puts every pixel in the class the global threshold did, which a 0/255 image always does and two-level noise with its levels more than 24 apart does (closer levels make every block flat; see the fifth round of "Phase 3, PR review round"), so those pay for the binarization and not for a third decode. The three passes (block statistics, the thresholded write, the count and the comparison with the global classes) have a 128-bit tier on net8.0 and later, held to the scalar form and to a reference written in the test. The decoder specs, the three spec maps, the shared-component list, the README and the public decoders' XML docs no longer call uneven lighting out of scope; hard-edged shadows, blur and strong perspective still are.

Tests: `UnevenLightingDecodeTest`, 71 per framework: a crisp symbol of each symbology under a ramp that takes 80 % of the light and under a shadow that takes 55 % across a three-module edge, eight directions each (40 of the 48 red before; the other 8 are ramps the global threshold already read), each case first asserting that the global threshold reads at least a tenth of the paper as ink; a symbol past correction under both lights does not decode; an unevenly lit image with no symbol is `NotDetected` in all three decoders; a short destination reports `DestinationTooSmall` through the regional pass; and both sides of the skip, two-level noise and a crisp damaged symbol agreeing with the global threshold, every lit case disagreeing. `LocalBinarizerParityTest`, 327: nine sizes (off a multiple of 8 on either axis, where the last block overlaps the one before), nine contents, four global thresholds, vector and scalar against the reference, and images under five blocks a side refused without a write. Mutations: a skip that always runs fails three tests, and the flat rule's comparison reversed fails 32. Full suite green on both frameworks.

**The renderer was checked against the corpus and against the other readers before it was trusted.** `UnevenLightingRenderer` (reflectance times light, crisp at 4 px/module) fails on `main` the way `qrcode-5` does, `NotDetected`. The first depths chosen were past what any reader reads reliably: at a shadow depth of 0.75 FeatherQR, zxing-cpp and ZXing.Net each read 2 of 8 Standard QR turns, and at 0.85 none do, because across a three-module edge the modules on both sides share one neighbourhood. At the tested depths every reader reads every turn, and FeatherQR reads at or above zxing-cpp at every depth and symbology probed (Micro QR shadow 0.65: 5 of 8 against 1; rMQR 0.8: 5 of 8 against 0). `main` reads no shadow at any depth.

**Three binarizations were probed as a fallback before one was built** (real images, after the decoder had failed): a local mean over a tenth of the image, 8 % under it; the same over an eighth, 5 % under; and the hybrid scheme. All three read `qrcode-5` and `qrcode-1` almost completely; the hybrid scheme read the most over the corpus (Standard QR 359 of 548 against 353 and 356) at about half the cost, needs no integral image, and is what the two readers ahead on photographs use.

**Exit met.** Real images, render for render against `main`: Standard QR 295 → 357 of 548 (+62, none lost; `qrcode-1` 24 → 31 of 32, `qrcode-5` 29 → 63 of 64, `qrcode-4` 36 → 49, `qrcode-2` 104 → 111, `qrcode-3` 44 → 45), Micro QR 60 → 64 of 64, rMQR 12 of 12; gap 153 → 92. The two residuals in the target sets are one turn each (`qrcode-1/17.webp` at 180°, `NotDetected`, a blurred dense symbol; `qrcode-5/01.webp` at 180°, `DataUncorrectable`, keystoned with large modules): the binarization of both is clean by eye, both read at the other three turns and under the two mean-window binarizations probed, so they sit on a margin of the finder and the frame, not of the threshold, and belong to phases 5 and 6. Synthetic sweep: +400 Standard QR reads and none lost, the gap 2,002 → 1,691 (bilinear upscales at 1.5-2 px/module 2,022 → 2,176, anti-aliased edges at 1.25-1.5 1,487 → 1,572 and at 1.5-2 2,637 → 2,655, keystone 6-20 % 2,439 → 2,513), Micro QR +45 (bilinear at 2-2.5 1,056 → 1,098), rMQR +11 at 640 cases; no misread anywhere. The finder is untouched, and the regional pass does not run on the two-level noise set (0/255), so its candidates are what they were.

**Lessons.** A second binarization is an added attempt, not a better first one: as a replacement the probe lost 14 corpus reads, because it has no grey levels for the finder's second look and moves edges the global threshold had right, and as an attempt after both polarities it lost none. The gains it made where nothing is unevenly lit (bilinear and anti-aliased renders below 2 px/module, keystone) say the same thing from the other side: a block-local threshold puts grey edge pixels differently, and a symbol one threshold misses by a few edges can read at the other. A skip that is exact beats a skip that is tuned: "every pixel where the global threshold put it" costs nothing to decide beyond the pass itself and cannot lose a read, where a fraction of changed pixels would have needed a bound and a sweep to defend it. And a stand-in renderer is a claim about the world that has to be checked against the world: the first shadow depths were chosen to make `main` fail and turned out to be past what every reader reads reliably, which the test would have pinned as a regression target nothing can meet.

**Benchmark delta.** Success paths unchanged (short job, in-process, `main` → branch): `v40-3px` 94.8 → 94.4 µs, `v40-3.4px` 111.8 → 105.5, `v40-4px-rot17` 305.8 → 288.7, `v40-4px-soft` 265.8 → 260.6, `v6-4px` 9.9 → 10.2, `M4_ImageDecode_Span` 5.1 → 4.9, `R7x43_ImageDecode_Span` 5.2 → 5.3, `R17x139_ImageDecode_Span` 19.3 → 20.7. No-symbol paths, where the image has grey in it and the pass runs: Standard QR `none-noise` (740 × 740 8-bit noise) 1.44 → 1.92 ms, `none-gradient` 324 → 540 µs, rMQR `NoSymbol_Gradient_1144x168` 105 → 197-204 µs; two-level noise, where it is skipped, `NoSymbol_Noise_1144x168` 6.87 → 6.73-7.06 ms. Failure paths, fastest of two interleaved rounds of five, at 512 × 512 unless named: skipped (two-level) Standard QR 1 px noise 0.90 → 0.99 ms, 2 px noise 0.21 → 0.32 ms, a crisp damaged v10 60 → 87 µs, Micro QR and rMQR noise within 5 % (their failure paths are 5-120 ms); run (grey) blurred noise Standard QR 0.35 → 0.61 ms and Micro QR and rMQR +6 to +7 %, a gradient 0.12 → 0.24 ms in all three, an anti-aliased damaged v10 63 → 131 µs and v25 215 → 440 µs, an anti-aliased damaged R13x99 20 → 48 ms, a Standard QR image read as Micro QR 40 → 72 ms and as rMQR 12 → 22 ms, and a textured ramp with no symbol 0.32 → 1.09 ms for Standard QR and 21 → 51 ms / 25 → 57 ms for Micro QR and rMQR, whose regional pass turns the texture into two-level specks and pays their finder's noise cost. The binarization alone is about 0.1 ms at 512 × 512 (it was 0.6 ms before its vector tier). A symbol that failed in the first two attempts now costs up to one attempt more, which is the trade this phase takes for the reads above; it was not narrowed further because every narrowing measured would have had to name a bound the corpus does not supply.

### Phase 3, cost round: the regional attempt scans strided only (2026-09-23)

*Undone by "Phase 3, second PR review" below: the regional attempts run the full sweep again.*

**Done.** Asked after phase 3 whether the doubled failure time could come down. Measured first where it goes: the regional attempt costs Standard QR 0.1 to 1.4 ms on a 512 × 512 failing image and Micro QR and rMQR 70 to 100 ms on noise-like ones (a textured ramp, 8-bit noise) and 42 ms on a Standard QR image read as Micro QR, so nearly all of it is those two decoders. Micro QR and rMQR run each attempt as a strided finder scan and, when it fails, a full sweep. The regional attempt now stops after the strided scan; the global attempts keep the sweep.

**Measured.** Reads, image for image against phase 3: the real images 624 of 624 identical, Micro QR 27,600 and rMQR 43,678 renders identical, and uneven light at 2 and 3 px/module (both symbologies, ramp and shadow, four depths, eight turns) identical. Failure paths, fastest of two interleaved rounds of five, as a multiple of `main`, phase 3 → this round: Micro QR textured ramp 2.38 → 1.50x, rMQR 2.28 → 1.53x, a damaged anti-aliased R13x99 2.35 → 1.69x, a Standard QR image read as Micro QR 1.78 → 1.45x and as rMQR 1.82 → 1.41x, a Micro QR image read as rMQR 2.31 → 1.67x, gradients 2.0 → 1.8x; two-level noise within 3 % of `main` as before; Standard QR unchanged (it has no sweep retry).

**The idea measured and dropped first: gating the attempt by how much the regional binarization disagrees with the global one.** Over every image the global threshold fails on, the share of pixels that change class does not separate the reads from the costs: the images the regional attempt reads range from 0.5 % (Micro QR photographs) to 51 % (`qrcode-5`), and the images it only costs lie inside that range (8-bit noise 0.6 %, another symbology's image 0.02 to 2.3 %, a textured ramp 40 %). Counting blocks with a quarter of their pixels changed instead separates them no better. Any bound would have lost reads or kept the cost.

**Lessons.** Where a cost goes has to be measured by component before it is cut: "the regional attempt doubles the failure time" was true of the total and false of Standard QR, whose share was a millisecond; the doubling was two decoders' second finder pass. A retry built for one attempt is not automatically worth running in the next: the sweep exists for finders the stride misses, and in an attempt that only runs once the two global ones have failed it found none. And a gate derived from the input is only as good as the separation it measures; the disagreement share looked like the natural gate and its distributions overlapped end to end.

**Left.** Micro QR's and rMQR's own failure path on noise-like images (70 to 120 ms per attempt at 512 × 512) is what every attempt pays, the global ones included, and is the lever for the rest. At 3 px/module zxing-cpp reads a few more unevenly lit Micro QR and rMQR symbols than this library (Micro QR shadow at depth 0.5: 6 of 8 against 8; rMQR ramp at 0.8: 5 of 8 against 7), which the synthetic kinds never drew; both are candidates for phase 7.

**Benchmark delta.** Success paths unchanged (the change runs only after both global attempts fail). The failure paths are above.

### Micro QR and rMQR failure path, investigated (2026-09-23)

**Investigated, nothing shipped.** Asked after the cost round whether Micro QR's and rMQR's own failure path could come down, since every attempt pays it. Sampled with `dotnet-trace` on 512 × 512 images with no symbol (1 px two-level noise, a textured ramp): Micro QR 108 / 62 ms a decode, rMQR 123 / 49 ms.

- **Where it goes.** rMQR: 92 % in `TryLocateSubFinder`, the 5 × 5 template search run for every frame whose finder-side format copy reads within 3 bits, which a quarter of random reads do. Micro QR: 97 % in the frames of false candidates, of which sampling the grid 28 %, codeword extraction 30 %, Reed-Solomon 13 %, the format read 8 % and transposes 5 %; a frame whose format reads within 3 bits (up to half of random reads) opens the scale and perspective searches, up to 10,000 decodes a candidate.
- **The full sweep is about 70 % of it** (without it: Micro QR 108 → 31 ms, rMQR 123 → 34 ms on noise). Its top eight candidates are mostly other noise than the strided pass's: skipping sweep candidates the strided pass already tried saves 10-20 % whether "already tried" means the same float position or within 2 px, so all of that is exact duplicates.

**Refuted, with the numbers.**

- **A tighter format gate in front of the searches.** Admitting only copies within 1 bit cuts noise to 18 ms (Micro QR) and 10 ms (rMQR), and loses 215 Micro QR and 457 rMQR renders of the sweep (within 2 bits: 57 and 170): anti-aliased and bilinear renders below 2.5 px/module and keystone past 6 %, whose format copy reads 2-3 bits off at the unrefined frame and whose refinement is what reads them. The real images lose nothing either way, which is why the sweep is the measure here.
- **The timing patterns as a second key for 2-3 bit frames** (Micro QR row 0 and column 0 from module 8; rMQR rows 0 and H-1 over columns 7-18, which alternate in every version). Over the frames that went on to read, the unrefined frame's timing was up to 71 % wrong for Micro QR and up to 100 % for rMQR (a rotated or keystoned frame is a module off within a few modules of the finder), overlapping noise frames end to end.

**What is left, all bit-identical in result.** Codeword extraction and grid sampling for Micro QR (58 % of its failure time between them; rMQR's extraction went 16-64x faster with a bit-plane kernel), and the sub-finder template search for rMQR (92 %), whose positions are independent and could be scored several at a time. The exact-duplicate skip is safe by construction (the same candidate, threshold and image give the same outcome) and worth 10-20 %. None changes a read, so each is a speed change with a parity test, not an accuracy one; they are phase 3b, in their own PR before phase 4.

### Phase 3, PR review round: the regional pass reads the negative too (2026-09-23)

**Done.** Two review comments on the PR, each put to three independent verifiers told to refute it.

- **Refuted 3/3: the vector block statistics fold min and max wrongly.** The comment read the 64-bit logical right shift as zero-filling the low bytes. It fills the high ones, which the fold never reads: byte 0 of each half ends as the half's minimum or maximum. One verifier ran the fold on a million random vectors against a scalar reduction with no mismatch; the parity test against the reference written in the test covers flat blocks on the vector path. Nothing changed.
- **Taken, and not as proposed: the regional pass had no negative.** A light-on-dark symbol under uneven light failed on the branch as it did on `main` (Standard QR ramp 1 of 8 and shadow 0 of 8 on both; Micro QR and rMQR the same turns on both), so it was a gap and not the regression the comment called it. The fix it proposed, inverting the positive's regional output, reads 2 of 48 such renders, because the flat-block rule reads a block as paper and the paper of a negative is dark. Binarizing the negative's own pixels reads 44 of 48. Each decoder now runs the regional pass on the positive and then on the negative (the inverted retry's buffer), each skipped when it agrees with its own polarity's global threshold, into a second rented buffer.

Tests: `UnevenLightingDecodeTest` gains the 48 lit renders negated (40 red before; the other 8 are the ramps the global threshold already read). Full suite green: 26,085 tests (378 skipped on this x64 machine), both frameworks.

**Measured.** Reads against the commit before: the real images +2 (`qrcode-2` 111 → 113 of 224), nothing lost; the three synthetic sweeps identical image for image (no synthetic kind draws a negative under uneven light). Failure paths, fastest of two interleaved rounds of five, against the commit before: textured ramp with no symbol Standard QR 1.09 → 1.80 ms, Micro QR 31 → 43 ms, rMQR 37 → 54 ms (a second regional decode); a gradient 0.22 → 0.32 ms in all three; two-level images, where both polarities are skipped, Micro QR and rMQR unchanged and Standard QR +0.1 to 0.26 ms (the second binarization; 1 px noise 0.99 → 1.25 ms); anti-aliased damaged Standard QR symbols 1.34x. The second decode on grey failing images is the price of reading negatives; phase 3b is where it comes down.

**Lessons.** A fix proposed with a finding is a hypothesis of its own: the gap was real and the suggested repair would have shipped a second pass that reads almost nothing. The asymmetry that sank it (paper is light) is the same rule that makes the regional pass work on the positive, so the negative needs its own pixels rather than the positive's answer inverted.

**Benchmark delta.** Success paths unchanged: the negative's regional pass runs only after both global attempts and the positive's regional pass fail.

**A second review round on that fix** (two reviewers, three verifiers per claim) left three findings standing. The regional retry is now one shared routine, `RegionalRetry`, which each decoder calls with a struct that runs its own attempt; the three copies had been identical, and a shared routine is what lets a test see its decisions. `RegionalRetryTest` (11) records each attempt's input: the positive's regional output first, the negative's own (not the positive's inverted) second with a histogram that matches it, terminal statuses from either returned, and nothing decoded for two-level noise at 0/255, 20/100 and 150/240, or under five blocks a side. Before it, two mutations passed the whole suite: handing the negative the positive's threshold, and decoding whatever the gate said; each now fails two tests. An image of only 0 and 255 is no longer binarized at all: every regional threshold then lies in [0, 251], so neither polarity can differ (three verifiers, one by proof and two over 3,920 and 554,112 passes, found no exception; the test pins the premise over 144 passes, both polarities, both tiers), which saves the two binarizations, about 0.17 ms at 512 × 512. And the spec bullet had kept the pre-negative cost next to the new one; it now gives the up-to-two-decode price with the textured-ramp numbers against `main`. A fourth claim, that the two Otsu searches repeat ones the attempts made, was not taken: 256 bins each, on the failure path.

**A third review round** (two reviewers, three verifiers per claim) left three more. The negative was binarized from the inverted buffer into a second rented one, which on runtimes whose shared pool stops at 1 MB (.NET Framework, .NET Core 5 and earlier) allocated an image-sized array on every failing large image; `LocalBinarizer` now reads the negative from the positive, each byte XORed with 0xFF as it loads (255 - v is v ^ 0xFF), so both polarities write into the inverted retry's buffer and the second rental is gone (`LocalBinarizerParityTest` runs every case in both polarities against the reference on a negated copy: 651). Three mutants of the retry survived: returning after a positive the gate skipped, dropping the histogram reset before a decode, and narrowing the 0/255 test by a bin; the last turns a QR symbol drawn in 0 and 1 beside a strip of 255 from read to `NotDetected`. `RegionalRetryTest` (14) now decodes only the negative on 0/100 cells in 16 px squares (flat 100 is paper to the positive, flat 155 is ink to the negative's global threshold and paper to the flat rule), checks each attempt's histogram against its input, and binarizes an image whose corner cell is 1 (or 254 among 0). And two texts said more than was true: a test summary promised the first attempt's status its assertions never checked (the status is pinned by the corner and timing-count tests), and the spec called all two-level noise free where only 0/255 is. Same reads as before this round, image for image.

**A fourth round** found no defect in the code: five mutants tried (the histogram reset, the negative's threshold, the flip on a vector load and on the scalar tail, stopping after the positive) each fail tests. Two texts were wrong and are fixed: the spec had the regional pass reading from the inverted retry's buffer, which it writes into, and `RegionalRetry.Decode` claimed to return the last attempt's status where a negative skipped after a failed positive returns `NotDetected`, which the decoders never report.

**A fifth round** found no defect in the code and six texts that said more than the code does: two-level noise is only free when its levels are more than 24 apart (closer ones make every block flat, measured on 100/120 noise: both polarities move pixels), a flat dark area moves pixels along the whole top or left edge and wherever it is more than half the lighter level, not only at the top-left, two test summaries claimed what their assertions did not (the data-destroyed test now also asserts an empty text), the retry's return doc left out its early exits, and the binarizer's summary said an evenly lit image never pays, where it is an image the global threshold reads.

**A sixth round** found no defect in the shared code and one gap in the tests: the Micro QR and rMQR uneven-light tests asserted the text and not the status, so dropping the `info = regionalInfo` hand-off or narrowing the terminal check to `Success` passed the suite while a caller saw the first attempt's `DataUncorrectable` on a successful read or on a short destination. Every uneven-light decode now asserts `Success`, and Micro QR and rMQR have the short-destination case Standard QR had. Four more sentences said more than the code does and were narrowed (which binarization reads a negative, the role of each polarity's threshold, "each decoded", and the cost and flat-block wording in the spec).

### Phase 3, second PR review: the regional attempt keeps the full sweep (2026-09-23)

**Done, and it undoes the cost round.** A review comment said the regional attempt's skip of the full-sweep finder retry (Micro QR and rMQR) could lose a finder whose band falls between the rows the strided scan visits. It could: `FindCandidates` scans every fourth row and has no rescan of its own (the rescan assumed here is in Standard QR's `TryFind`); the widening is the caller's full sweep, and the finder search documents that no decoder's envelope is narrower than a full sweep's. One verifier built the failure directly: unevenly lit Micro QR and rMQR renders at 1.5-2 px/module, turned and offset by half pixels, 880 a cell, of which 30 to 47 read only with the sweep and none only without it. The regional attempts now run the sweep again. `UnevenLightingDecodeTest.UnevenLight_FinderBetweenScannedRows_Decodes` holds six renders of that kind at 2 px/module (Micro QR and rMQR, two crisp and one anti-aliased each), red with the sweep skipped. `UnevenLightingRenderer` gained the form they need: any turn and a pixel offset, crisp or with 4 × 4 anti-aliasing.

Three documentation comments were verified alongside: the phase-3 entry's "a two-level image always" skips the pass was false even when written (levels 24 or less apart make every block flat) and is corrected above; its test totals (71, 327) and its 357 are right for their date (the test classes now hold 127 and 651 cases a framework, and a later entry records the negative's +2 to 359), and the spec now says its 359 is measured against `main`.

**Measured.** The verifier's probe re-run on this source reads exactly what its swept variant read (Micro QR 2 px/module crisp 698 of 880, anti-aliased 368; rMQR 1.5 crisp 347, 2 anti-aliased 463). Failure paths, fastest of two interleaved rounds of five, as a multiple of `main`: textured ramp Micro QR 4.08x (21 → 88 ms) and rMQR 4.52x (25 → 113 ms), blurred noise about 2x, a Standard QR image read as Micro QR 1.92x and as rMQR 2.20x, a damaged anti-aliased R13x99 2.32x; two-level noise unchanged; Standard QR unchanged against the cost round (its finder search widens inside `TryFind`, which that round did not touch; against `main` it pays the regional attempt itself, 0.32 → 1.80 ms).

**Lessons.** "It read nothing more over every set measured" is evidence about the sets, and none of them could have shown the loss: a render shows it only when it both needs the regional attempt and has a finder between scanned rows, and the sets that turn a symbol or offset it are evenly lit (the global attempts, which keep the sweep, read them first: at 2 px/module, evenly lit and turned 1-89°, 33 to 85 of 1,080 renders a cell need the sweep), while the unevenly lit set was axis-aligned (its eight turns were light directions) and no real image had the case. A cut that removes a fallback needs the case the fallback exists for, built on purpose, not an absence of it in the sweep. And an assumption carried over ("stride with a confirmation-triggered rescan") was true of a different entry point; the code, not a remembered summary, says what a caller relies on.

**Benchmark delta.** Success paths unchanged: the sweep runs only in the regional attempt, after both global attempts fail. In-process, fastest of two interleaved rounds, the commit before → this: Micro QR `M4_ImageDecode_Span` 4.71 → 4.69 µs, rMQR `R7x43_ImageDecode_Span` 4.96 → 4.90 µs, `R17x139_ImageDecode_Span` 18.85 → 18.79 µs, `R7x43_BitmapDecode` 7.08 → 7.08 µs, `R17x139_BitmapDecode` 29.7 → 30.5 µs (+2.7 %, within noise), allocations unchanged. The failure path pays the sweep again: `NoSymbol_Gradient_1144x168` 249 → 275 µs (+10 %), `NoSymbol_Noise_1144x168` 6.45 → 6.43 ms (0/255 noise, so no regional pass).

**A review round on the test** found two of the six renders sound only by accident: Micro QR turned exactly 180° at a half-pixel x offset puts most crisp pixel centres exactly on a module edge, so which module each shows follows the sign of `MathF.Sin(π)`'s residue and some modules come out 1 or 3 px wide; at 0.49 or 0.51 they read without the sweep. They were replaced by crisp renders with no pixel centre on an edge that still need the sweep (37° at (0.5, 1.5) under the shadow, 53° at (0.5, 2.5) under a downward ramp), all six red with the sweep skipped. The renderer's unused fractional-scale form was removed and its turned form's summary says what its offset is (pixels, whole or fractional, from a point 1.5 px above the centre). Lesson: a render parameter at an exact tie (half-pixel offset at a right-angle turn) tests the rounding, not the geometry; a case that needs a fallback has to keep needing it when nudged.

**A second review round on the test** found the replacements still narrow: rMQR 20° at (0.5, 1.5) read only within 0.05° of its turn and a few hundredths of a pixel of its offset even with the sweep, and four of the other five had nudges that read without the sweep or not at all. A search over 12,960 renders (2 px/module, both symbologies, crisp and anti-aliased, turns 1-89°, three x and eight y offsets, three lights) scored each render that needs the sweep against 20 nudges (turn ±0.1/0.2/0.5°, each offset ±0.02/0.05/0.1 px, depth ±0.03); the six now in the test read with the sweep and fail without it at all 20, and all six are red with the sweep skipped. The renderer's class summary no longer calls every render crisp at a whole scale, the turned form states the offsets that keep its two-module margin, and the shared spec-map row names the full sweep for Micro QR and rMQR only (Standard QR's attempt widens inside `TryFind`). Lesson: a regression case has two conditions, and the one forgotten is that the path under test must still read around the chosen point; a case that fails when nudged fails for the next unrelated change.

**A third review round on the test** found the 20-nudge search too coarse: sampled densely (turn every 0.025° to ±0.5°, each offset every 0.005 px to ±0.1 px, depth every 0.005 to ±0.03), three of the six read without the sweep between the sampled nudges, and two failed even with it where two nudges combine (turn and depth). The candidates were checked again at those 136 axis steps and the 81 corners of the nudge box; twenty pass both everywhere, none of them under the shadow or anti-aliased rMQR, and the test now holds five of them plus rMQR anti-aliased 33° at (0, 2.5) under a downward ramp, which passes every axis step and reads without the sweep at 3 of the 81 corners. All six are red with the sweep skipped. The first review's reason for the earlier no-loss results was also wrong and is corrected above and in the spec: the turned and offset sets were evenly lit, so their finders between scanned rows were read by the global attempts. A probe from the same review found a second way to need the sweep: eight or more finder-like marks outrank the real finder in the strided scan's candidate list, which tries eight, and 1,254 of 12,288 renders with ten decoys read only with the sweep. Lesson: a robustness claim is only as fine as its sampling; state the grid it was checked on, or check it densely.

**A fourth review round on the test** confirmed the cause per case: on each render's regional binarization the strided `FindCandidates` returns no candidate, and the full sweep returns one, the real finder, so no decoy crowds it out. The test comment now claims robustness per axis only (the anti-aliased rMQR case reads without the sweep at 15 of 625 points of a 5 × 5 × 5 × 5 nudge grid), the renderer's class summary says which form needs the quiet zone in the grid, and the cost-round entry points here.

**A fifth review round on the test** found the test trusted its premise: it asserted a decode only, so a later change that let the strided scan read these renders would leave it green and guarding nothing. Each case now first asserts, on its regional binarization, that the strided `FindCandidates` finds no finder and the full sweep does (an axis-aligned control fails there), and asserts `Success` like the other uneven-light tests. The comment states the nudge range, the summary no longer reads as if the global attempts read these renders, and the spec's 880 is per set.

**A sixth review round on the test** found the other half of the premise unchecked: nothing asserted that the global attempts fail on these renders, and if one read them the regional attempt would never run. Each case now also asserts that neither global polarity decodes (`DecodeLuminanceCore` of each decoder, strided scan and sweep, is internal for it; an evenly lit control fails there).

### Phase 3, branch review: limits stated, premises exact (2026-09-23)

**Done.** An adversarial review of the whole branch against `main` (the binarizer against an independent port of ZXing's `HybridBinarizer` on 20,000 random cases in three SIMD configurations: no mismatch) left eight findings after three votes each:
- **A soft shadow is read only while it takes less than half the light when the modules line up with the 8 × 8 blocks.** At 8 and 16 px/module, whole pixels, every block is flat and takes the lit side's black point (paper 220 → 110), so a shadow taking half or more reads its paper as ink and only 4 to 6 of 8 directions decode (ZXing.Net's hybrid binarizer the same without its rotation retries; zxing-cpp reads none at 8 px). The public remarks, README and spec were narrowed to a shadow under half the light (and in the second round below to 40 %). Reading past half on aligned grids is a candidate for later work, not a defect of this port.
- **16 of the 96 lit decode cases never needed the regional pass**: the global threshold reads Standard QR ramp 45°, Micro QR ramps 45/135/315° and rMQR ramps 0/45/135/315°, and their premise (a tenth of the paper read as ink) did not notice. Each lit case now asserts which pass reads it, both ways, through the decoders' internal `DecodeLuminanceCore` (Standard QR's made internal too); a later change that lets the global threshold read more turns the list red instead of silently.
- **The flat rule's two boundaries were untested**: `range <= 24` → `< 24` and `min < neighbours` → `<=` both passed the whole suite. The parity test gains levels 24 and 25 apart and a flat block at its neighbours' level (867 cases); each mutation now fails 72.
- **`charsWritten` leaked from the attempt that got furthest**: a mirrored negative under uneven light reaches its bitstream only in the last regional attempt, and a bitstream that fails after writing text returned false with five characters written. `main` had the same leak through the inverted retry. Every decoder now reports 0 on total failure (`StandardQR_UnevenLightBitstreamFailsPartWay_WritesNoCharacters`).
- **Images under 33 px a side get no regional pass** (no 5 × 5 neighbourhood of blocks; ZXing stops at 40), now stated in the public remarks and the spec.
- Four texts: the spec's "no reader past 65 %" (every reader reads 2 of 8 at 75 %, none at 85 %), the sweep's +400 mixing Micro QR into Standard QR's total, "well-lit" left in `FinderPatternFinder`, and the flat rule described as "top and left" where it is the 1:2:1 mean of top, left and top-left.

Not kept: the skip being unproven when grey levels give the finder a second look (no reproduction).

**Lessons.** A premise that is necessary is not sufficient: "the global threshold reads part of the paper as ink" was true of cases the global threshold still read. State which path reads a case and assert it both ways. And a reference written from the same description as the code shares its boundaries: a parity test only guards a boundary its inputs sit on.

**A second round** found the first round's own promise false and three more gaps, each kept after three votes:
- **"Under half the light" failed below half.** Swept 0.20-0.65 over Standard QR versions 1-5 at every level, Micro QR M1-M4 and rMQR R7x43, R11x27 and R13x43, 2-16 px/module, eight directions: every direction reads up to 42 %, and the first failures are at 43 % (Standard QR version 1-H on 8 px modules) and 44 % (2 px). The test at 0.45 passed only because its version 3-M symbol is among the strongest, and the global threshold read all 48 of its cases, so it guarded nothing of the regional pass. The public docs now promise a soft-edged shadow of up to 40 %; `UnevenLight_ShadowUpToFortyPercent_Decodes` (168: three symbologies, seven scales, eight directions, Standard QR at version 1-H) holds it, and `UnevenLight_ShadowPastHalfOnBlockAlignedModules_RegionalPassDecodes` (4, at 55 % on 8 px modules) asserts the global attempts fail and the regional pass reads.
- **A content verdict was lost to "report the first attempt".** A symbol only the regional pass reaches, holding a Kanji cell with no JIS X 0208 mapping, reached the caller as `NotDetected`, not the `UnmappedCharacter` a caller branches on to fall back to a CP932 reader; twice over, since the retry also returned the negative's `NotDetected` over the positive's verdict. `main` had the same loss for a reversed symbol through the inverted retry. A verdict on the symbol's content (`UnmappedCharacter`, `UnsupportedContent`) now outranks an attempt that found nothing, in the retry and in each decoder's report (`RegionalRetryTest.Decode_ContentVerdict_OutranksANegativeThatFoundNothing`, `KanjiUnmappedCharacterEndToEndTest.UnmappedCell_ReadOnlyByALaterAttempt_ReachesTheCaller`).
- **The image path's `charsWritten` of 0 on failure was undocumented**, and differs from the matrix path, which reports the partial count; the three `TryDecodeImage` span overloads now say so. The Micro QR and rMQR resets never change a value their scans already zero; they stay as the place the contract is kept.
- Three texts: the black point "from its own range" (it is the mean), the Tier 1 row without the 33 px condition, and one three-module shadow edge called soft in the public docs and sharp in the spec (now soft-edged throughout, the measured edge named).

**Lessons.** A bound checked on the test's own symbol is a bound on that symbol: the worst case sat two versions away. And a test that promises to exercise a path has to assert that it does; the first round wrote that rule and then broke it in its own new test.

**A third round** found the second round's `charsWritten` contract false and its status rule half-built, each kept after three votes:
- **`DestinationTooSmall` returned a partial count.** A Standard QR of 40 digits then "abc" (Optimal segmentation) into a 40-character destination returned false with 40 written: the status is terminal, so the reset on total failure never ran. Each decoder's `DecodeLuminance` is now a wrapper that reports 0 on any status but `Success` (`StandardQR_DestinationShortOfTheSecondSegment_WritesNoCharacters`), which also retires the Micro QR and rMQR resets that never changed a value.
- **A first attempt's content verdict still ran every other attempt**, the regional pass included (two binarizations on any image with grey), then reported the first status anyway: roughly twice the time for every GS1 or unmapped-Kanji symbol. A verdict now ends the attempts wherever it is reached (first, inverted, regional); only another symbol in the image could have changed the answer.
- **The rule had no test outside Standard QR and one unguarded branch.** `ContentVerdictImageDecodeTest` builds Micro QR M4-L and rMQR R11x43-M symbols with a Kanji cell from real codewords and reads the unmapped cell as `UnmappedCharacter` through each path (first, inverted, regional), with a mappable control; `RegionalRetryTest` adds a negative `Success` and `DestinationTooSmall` after a positive verdict.
- **`InvalidBitstream` and `DataUncorrectable` stay out of the verdicts**, now said where the rule is: noise reaches the second in the regional pass (the no-symbol test turns red when it counts), and the first through a miscorrection, notably on Micro QR M1, whose codewords only detect errors. The rule is in the spec (`qrcode-symbologies.md`, next to `UnmappedCharacter`), and the texts that said a verdict outranks "an attempt that found nothing" say "a failure short of the content".
- The lighting paragraph said the global threshold stops past 45 % and the first failure is at 43 %: the first is the version 3 symbol tested, the second a small version. Both are now attributed, and the test's own symbol first fails at 44 %.

**A fourth round** found the third round's early exit a regression and the verdict rule broken inside the attempts it did not touch, each kept after three votes:
- **The first-attempt exit lost a read `main` made.** Beside an unmapped symbol, a light-on-dark readable one is read by the inverted retry; ending on the first attempt's verdict returned `UnmappedCharacter` where `main` returned the text, in all three symbologies. The first attempt and the inverted retry now run as on `main`; a verdict from either skips only the regional pass, which looks for a symbol the global threshold cannot see, and a verdict proves it was seen. That keeps the saving the third round was after (no binarization for a GS1 or unmapped-Kanji symbol) without changing any read. In the regional pass a positive verdict now ends it, so `RegionalRetry` lost its preference branch (`ContentVerdictImageDecodeTest.UnmappedSymbolBesideAReversedReadableOne_ReadsTheReadableOne`, `RegionalRetryTest.Decode_PositiveContentVerdict_StopsThere`).
- **Inside an attempt the verdict still lost, as on `main`.** Micro QR and rMQR rank failures across the grids they try for one symbol, and ranked a verdict equal to `DataUncorrectable`, keeping whichever came first: a Micro QR M4-L unmapped symbol turned 7° reported `DataUncorrectable` in 59 to 69 of 72-92 renders. Standard QR's mirror retry kept the unmirrored grid's `FormatInformationInvalid` over the mirrored grid's verdict. A verdict now ranks with `DestinationTooSmall`, and the mirror retry prefers it (`UnmappedCell_Turned_ReachesTheCallerAsUnmappedCharacter`, `StandardQR_UnmappedCellMirrored_ReachesTheCallerAsUnmappedCharacter`).
- **The searches inside Micro QR and rMQR attempts keep going after a verdict** (2-19 ms for an unmapped symbol, as on `main`): speed with no result to change, so it joins phase 3b's levers.
- Texts: the flat bound (a range of exactly 24 is flat), the 12 px shadow bound (59 %, not 56 %) and the sweep's payload ("12345"), and the rMQR pipeline diagram and chain, which ended at the inverted retry.

Seen on the way and left for its own work: a Micro QR symbol turned 30° holding Kanji 0x8794 decodes as `Success` with "95549", a false read of another grid, on `main` too.

**A fifth round** found the rule still broken inside attempts and one regression from the fourth, each kept after three votes; a sweep against `main` found no read lost (134,878 synthetic renders and the 624 corpus images: 0 lost, 84 corpus reads gained, `qrcode-5` misreads 16 → 0) and no verdict from noise:
- **Fallbacks inside an attempt still dropped a verdict**: Standard QR's timing frame, other dimensions, mesh and finder-only grid, and the full finder sweep of Micro QR and rMQR, each let a verdict lose to the failure of the grid chosen against it (the timing frame and the mesh run first, and a later grid's failure overwrote their verdict; the others ran after a failure and were not returned). 52 of 1,476 low-density renders (1-3 px/module, crisp and anti-aliased) reached `UnmappedCharacter` inside and reported `DataUncorrectable` or `NotDetected`. Each decoder now has `IsSettled` (read, too long, or a verdict) at those points (`UnmappedCell_ReadByAFallbackInsideTheAttempt_ReachesTheCallerAsUnmappedCharacter`, which also pins the rMQR ranking the fourth round left untested).
- **The fourth round's mirror-retry exit broke an invariant**: returning on an unmirrored verdict before transposing left the finder-only fallback comparing its sampling against the wrong orientation, one redundant decode per verdict on version 2+. A verdict now returns before that fallback, so the comparison only ever sees a transposed first grid.
- The wrapper's comment named a short destination and a verdict; the case its test guards is a later attempt that failed part-way through its bitstream.

Seen on the way, on `main` too, and left with the other false read for its own work: a Micro QR M4-L unmapped symbol, anti-aliased at 1.60 px/module, offset 0.66, reads as version M1 "0824" (M1 only detects errors).

**A sixth round** found the fifth's own change too wide, each kept after three votes:
- **A strided verdict skipped the full sweep and lost a readable neighbour.** Two symbols side by side, an unmapped one the strided scan finds and a mappable one at 1.0-2.5 px/module only the sweep finds: Micro QR read 422-430 of 488-496 pairs against `main`'s 442-450, rMQR 424-432 against 448-454, every loss `main`'s `Success` turned `UnmappedCharacter`. The strided scan returns early on a terminal status again; the sweep's verdict is still reported (`UnmappedSymbolBesideALowDensityReadableOne_ReadsTheReadableOne`).
- **Four Standard QR sites had no test that could reach them**: the timing-counted, version-named and runner-up dimensions and the finder-only grid, reverted one at a time, changed 2-60 unmapped renders each and failed nothing, because the only Kanji builder made version 1. It now builds any version (blocks interleaved, version information placed from 7), and `StandardQR_UnmappedCellReadByALaterGrid_ReachesTheCallerAsUnmappedCharacter` holds one render per site (versions 5, 3, 2 and 8).
- Texts: the spec called the timing frame and the mesh fallbacks after a failure (both run first), said a verdict "settles the search the way a read does" (the Micro QR and rMQR scans still run their candidates), and three comments still said `DestinationTooSmall` outranks every failure, which a verdict now shares.

**Lesson.** A rule stated for "the symbol" has to be checked against the image: stopping a search because one symbol gave a verdict also stops it for every other symbol in the image, and the envelope contract (never narrower than a full sweep's) is about the image.

**A seventh round** found no defect in the decode logic and four gaps, each kept after three votes; a probe of 8,090 images (all three decoders, single and two-symbol, noise) lost no read against `main`:
- **The mesh's verdict had no test**: reverting it failed nothing, yet a version 35-L unmapped symbol keystoned 11 % is read only by the mesh (`StandardQR_UnmappedCellReadByTheMesh_ReachesTheCallerAsUnmappedCharacter`, which the Kanji builder reaches now that it builds any version).
- **The regional pass gave Micro QR false reads `main` did not**: inverted, turned, shadowed rMQR images read by the Micro QR decoder as M1 symbols (M1 only detects errors), 12 of 6,400 against `main`'s 3, the 9 extra all through the regional pass. The Micro QR regional attempt now refuses an M1 result (`MicroQR_UnevenLightRmQRImage_NoFalseRead`); the user chose that over keeping the reads. It costs 36 Micro QR sweep reads the pass had added (bilinear upscales of M1 at 2-2.5 px/module), so the Micro QR sweep gain against `main` is +9, not +45; the corpus is unchanged (60 → 64). Recovering those reads with a structural M1 check belongs with the Micro QR false reads `main` already has, set aside for their own work.
- **Corrections to earlier entries**: the fifth round's "84 corpus reads gained, `qrcode-5` misreads 16 → 0" compared against a `main` run whose expected texts had CRLF where the decoded ones have LF; against the canonical baseline the corpus gains 68 (Standard QR 64, Micro QR 4) with no misread on either side. The fourth round's "speed with no result to change" for the searches after a verdict was disproved by the sixth, and the 3b row no longer lists that stop as a lever.
- Texts: `LocalBinarizer` joins the SIMD tier inventory, the Micro QR and rMQR envelope statements carry the lighting support the public docs promise, the spec says that a verdict skipping the regional pass also skips a second, unevenly lit symbol beside it, and the Kanji test's summary no longer says every failure retries.

**An eighth round** found the M1 refusal incomplete and its documentation too broad, each kept after three votes; a random mix of 21,000 images and a focused 90,000 lost no read against `main` and gave no new Standard QR or rMQR misread:
- **An empty M2 read still got through**: an inverted, turned, shadowed rMQR image read by the Micro QR decoder as M2 with no text, 2 errors corrected toward an all-zero data word (numeric, count 0), about 1 in 30,000 focused renders, `main` reading nothing. The Micro QR regional attempt now also refuses a successful read with no text (`MicroQR_UnevenLightRmQRImage_NoEmptyRead`); `DestinationTooSmall` stays, since a short destination reports no text either.
- **The 40 % promise is not M1's**: the global threshold alone loses a turned or anti-aliased M1 symbol from about 30 % (a 2.2 px/module render turned 322° from 29 %), on `main` too, where the regional pass had carried it to 40 %. The public docs and the spec now scope lighting to M2 and larger, and the spec's ramp sentence to the symbols that read at 80 % (rMQR R7x43 loses 2 of 8 directions).
- **Two tests pin the refusal's edges**: an M1 symbol under a shadow is not read by the regional pass even into a short destination (`MicroQR_M1UnevenLight_NotReadByTheRegionalPass`), and an M2 symbol is (`MicroQR_M2UnevenLight_ReadByTheRegionalPass`); before, narrowing the refusal to `Success` or widening it to M2 passed the suite. The refusal's state resets were dead (the negative polarity or the caller overwrote them) and are gone.
- Texts: the Kanji test's summary (the inverted retry runs on any failure), a plan identifier on an rMQR spec line this branch rewrote.

**A ninth round** kept four findings after three votes each and dropped one (a content verdict still costs Micro QR about 10 ms and rMQR about 2 ms, the same on `main`: the candidate scans this branch did not change search every refinement of a finder whose grid gave a verdict):
- **The 40 % promise held only on crisp renders**: the shadow was measured with the whole-scale renderer, crisp and axis-aligned. Anti-aliased edges hold 40 % from 3.5 px/module, turned or not (0 of 7,040 directions lost from 3.5 to 6 px/module over Standard QR versions 1-5, Micro QR M2 and M3 and three rMQR versions, turns to 45°); at 3 px/module a symbol turned 45° loses a direction or two from 30 %, at 2.5 px/module 222 of 1,760 directions are lost at 40 %, and at 2 px/module losses start at 20 %. The public docs and specs now carry "from 3.5 px a module when edges are anti-aliased", and `UnevenLight_AntiAliasedShadowUpToFortyPercent_Decodes` pins it (three symbologies, three turns, eight directions at 3.5 px/module).
- **A refused M1 read ended the Micro QR regional attempt**: the refusal judged the attempt's result, so an M1 grid read first stopped the candidate loop and skipped the sweep, and an M3 beside it that only the regional pass reads was lost (46 of 144 pair images at 45-65 %, `main` reading neither). The refusal now judges each grid inside the scan, which goes on to the next grid or candidate, and all 46 read the M3 (`MicroQR_M1BesideM3UnevenLight_RegionalPassReadsTheM3`); the Micro QR sweep against `main` is unchanged (+9, none lost, no misread).
- **A verdict skipping the regional pass had no test**: removing the first attempt's check passed the suite in all three decoders, and it is observable (beside an evenly lit unmapped symbol, a shadowed readable one is then read). `UnmappedSymbolBesideAShadowedReadableOne_ReportsTheVerdict` pins the documented behaviour.
- **The Micro QR `charsWritten` reset had no test**: a reflectance-reversed M1 under a shadow reached the caller as `NotDetected` with 5 characters counted once the reset was removed. With the refusal inside the scan every failure already counts 0, so the reset is now a guard; `MicroQR_M1UnevenLight_NotReadByTheRegionalPass` covers both polarities with the span overload. The refused text can stay in the destination, so the public docs now say the destination may hold text "from an attempt that was not kept".

**A tenth round** kept seven findings after three votes each:
- **The 40 % promise needed a density floor and a margin**: a crisp symbol under 2 px/module can lose every direction at 40 % (1 to 1.75 px/module, where the spec says crisp renders read), and a margin wider than the quiet zone makes the global threshold split lit paper from shadowed paper, which the regional pass does not make up for on block-aligned modules (8 px/module from 38 % with an 8-module margin, from 26 % with 20 to 30). Within the new wording, "from 2 px a module or 3.5 when edges are anti-aliased, with no more margin than the quiet zone", a sweep of Standard QR versions 1-5, Micro QR M2-M4 and three rMQR versions at both levels lost 0 of 8,016 crisp and 0 of 19,200 anti-aliased directions at 40 %. The spec's numbers were corrected with it: anti-aliased losses at 2 px/module start at 17-20 % axis-aligned and 10 % turned, not 20 %; crisp turned symbols at 2 px/module lose a direction at 40 % in more cases than the one named; the deeper-shadow limits are 56, 61 and 59 % on whole-scale renders and 55 and 57 % with sub-pixel offsets.
- **A refused M1 cost the whole search**: judging each grid let the arbitrary-orientation, scale and perspective searches go on sampling the same finder, about 2,000 to 17,000 grid decodes, so an M1 symbol under a shadow failed in 3-6 ms where `main` fails in 0.03 ms. A refused grid now ends its candidate's search, and the scan goes on to the next candidate: 0.09-0.66 ms, with the M1-beside-M3 read kept.
- **Real symbols with no text were refused**: the empty-read refusal took a Micro QR M3 or M4 symbol holding no text, a valid symbol, for texture (159 and 174 of 480 anti-aliased renders read only by the regional pass lost). The refusal now needs the read to have corrected errors, which the known false read did (2) and a real empty symbol read cleanly does not (`MicroQR_EmptySymbolUnevenLight_ReadByTheRegionalPass`).
- **The refusal on the low-density path had no test**: the module-boundary grids under 1.5 px/module passed 593 more regional M1 reads with their refusal removed, and the suite stayed green; `MicroQR_M1UnevenLightLowDensity_NotReadByTheRegionalPass` pins it.
- **The failure-path multiple was quoted on an image `main` already paid for**: phase 3b now also names the noisy ramp `main` rejects in 0.2 ms, 80-200x on this branch.
- Set aside, reproduced on `main`: a crisp, evenly lit Standard QR version 1 symbol at exactly 1.95 px/module fails at levels L, M and Q (1.93, 1.94, 1.96 and 1.97 read).

**An eleventh round** kept nine findings after three votes each:
- **A number in the public docs kept needing conditions it did not carry**: after density, anti-aliasing and margin, this round found rotation (a crisp symbol turned at 2 px/module is not read even unlit at some placements) and contrast (every measurement used ink at 30 on paper at 220; ink at 60 on paper at 200 holds to about 37 %, ink at 100 on paper at 170, the low contrast the spec maps list as supported, to about 23 %, no further than `main` at the weakest scales), and "margin" was undefined. The user chose to drop the number from the README and the public decoders' remarks, which now say "a shading gradient or a soft-edged shadow over part of the symbol", and to keep every measured limit in the decoder spec's lighting section, now a list by rendering (crisp along the axes from 2.6 px/module, crisp turned from 3.6, anti-aliased from 4.5 and in all but 3 of about 3,600 turned renders from 3.5) with the contrast, the crop and the placements it was measured under.
- **The candidate stop cost real empty symbols**: an empty symbol read with corrections on its first grid was refused and its candidate ended, so the grid that reads it cleanly was never tried (268-524 renders lost against no stop, 5-19 within the measured limits). Only an M1 read ends the candidate now; the other refusals let the search go on (`MicroQR_EmptySymbolTurnedUnevenLight_ReadByALaterGrid`), with the same cost on M1 (8 and 46-224 grid decodes).
- **The regional pass read other symbologies' texture as M3-M at its correction limit**: 4 in 120,000 turned, shaded Standard QR and rMQR images decoded as Micro QR, digits or letters with 4 of 4 corrections, which `main` does not read (it has its own 6 non-M1 false reads of this class on those images, besides its M1 ones). The regional pass now refuses an M3 or M4 read that needed every correction its level allows (`MicroQR_UnevenLightForeignImage_NoReadAtTheCorrectionLimit`), without ending the candidate; the verifiers measured under 1 % of the pass's Micro QR gain lost that way (2-3 renders within the measured limits, all M2-L, which this rule leaves out).
- **Nothing guarded the candidate stop**: removing it passed the suite. `MicroQRImageEndToEnd.M1_UnderShadow_ImageDecode_Fails` measures that failure path: 24 us, 857 us without the stop.
- **A refused grid's transpose was still decoded** on the fast path; it no longer is, as in the refinement searches.
- Texts: the spec's empty-read sentence (only an empty read that needed correction is refused), the Micro QR spec map's M1 sentence (turned or anti-aliased, as the decoder spec says).

**A twelfth round** kept findings in the spec's lighting numbers, the refusal and its guards:
- **The lighting section's finer numbers were artifacts of one sweep each**: other placements, payloads and margins moved the onsets (anti-aliased at 2 px/module from about 4 % at some sub-pixel placements, not 17; a wide margin losing renders from about 27 % at 2-4 px/module, which the section said the regional pass made up for; low contrast reading no further at 6 px/module, where the section said 43-45 %; M1 anti-aliased from about 17 %). The section now states its measuring conditions first and keeps only the bounds every sweep held to, conservatively, and the Micro QR spec map follows it.
- **A verdict escaped the correction-limit refusal**: the refusal judged reads (`Success`, `DestinationTooSmall`) only, so a grid on another symbology's texture that reached an unmapped Kanji cell at M3-M's limit reported `UnmappedCharacter` (and `NotDetected` into a one-character destination), where `main` reports `NotDetected`. Verdicts are judged like reads now (`MicroQR_UnevenLightForeignImage_NoVerdictAtTheCorrectionLimit`); a real unmapped symbol at its limit under a shadow gives `DataUncorrectable`, as a mappable one already did.
- **The refinement searches' stop and the rule's edges had no guard**: dropping the stop in the rotation, scale and perspective searches turned a turned M1 failure from 1.5 ms into 7.6 ms with the suite green, now measured by `MicroQRImageEndToEnd.M1_TurnedUnderShadow_ImageDecode_Fails`; widening the rule to M2, to one correction short of the limit, or to short destinations passed too, now pinned by `MicroQR_DamagedUnevenLight_ReadByTheRegionalPass` (M2-L at its limit, M3-L and M4-L one short, each into a short destination). The foreign-image test asserts its premise: without the refusal those images read at the limit.
- **The refusal's cost was quoted from a wider rule**: the shipped rules cost no read within the lighting bounds, and past them, under deeper shadows, up to about 2 % of the Micro QR gain (the correction-limit rule alone 0.3 %, the empty-read rule about 1 % with one payload in four empty).
- Texts: two comments that said every refusal ends the candidate (only M1 does).

**A thirteenth round** kept three findings:
- **The correction-limit rule cost real M4 reads within the lighting bounds**: an M4 symbol that sampling already put at its limit (3 of 3 on M4-L, 5 of 5 on M4-M) at one turned placement was refused, 1 or 2 renders in 10,000 crisp or anti-aliased at 40 %, and the same for an unmapped Kanji verdict. Every false read at a limit the regional pass added was an M3-M (a random word falls within its 4 corrections at least 14 times as often as for any other level, and one M3-L symbol was lost at its limit too), so the rule was narrowed to M3-M, then widened to M4-M once a verifier found one M4-M false verdict at 5 of 5 corrections in 150,000 foreign images (`MicroQR_UnevenLightForeignImage_NoVerdictAtTheCorrectionLimit` gained it); every foreign-image guard holds. `MicroQR_DamagedUnevenLight_ReadByTheRegionalPass` gained an M4-L and an M3-L symbol at their limits (each red under the wider rule).
- **The turned bounds were 2 points high for one family**: Standard QR version 1 (mostly Q and H) turned near 45° with the light along the diagonal loses up to about 1 render in 200 from 38 %, crisp from 3.6 px/module and anti-aliased from 4.5, and from 36 % once the margin is 2 modules wider than the quiet zone. The spec's turned and margin bullets say so.
- **A one-character read's refusal edge had no test**: widening the empty-read refusal to one character passed the suite; the damaged-symbol test now has a one-character M3-L symbol that needed a correction.

**A fourteenth round** kept seven findings:
- **A false verdict from the global threshold hid a readable symbol**: an M3-M or M4-M "12345" under a 40 % shadow at one turned placement gave the global attempt an `UnmappedCharacter` at M3-M's limit of 4, which skipped the regional pass that reads it cleanly; `main` reports `DataUncorrectable` there. A Micro QR verdict at level M's correction limit no longer skips the regional pass: a read there is reported, otherwise the verdict (`MicroQR_GlobalVerdictAtTheCorrectionLimit_RegionalPassReads`, and `MicroQR_UnmappedCellAtTheCorrectionLimit_ReachesTheCallerAsUnmappedCharacter` for a real M4-M verdict at its limit).
- **The M4-M limit rule has a cost**: about 1 in 15,000 anti-aliased M4-M renders at 3.5-4.5 px/module under a 40 % shadow, none measured crisp or from 4.5. The spec states it.
- **Lighting text**: anti-aliased Micro QR M4 turned near 220-225° with the light toward 45° loses a few renders in a thousand at 40 %; low contrast holds to about 30 % (60/200) and 17 % (100/170), with a few turned renders in a thousand lost beyond; the deeper-shadow sentence is scoped to whole pixels along the axes; the sentence comparing with `main` is gone.
- Texts: the damaged-symbol test's summary (M4-L, not M4, at its limit), the Micro QR benchmark summary (it measures decode too).

After this round the user asked for comments to be written only once the behavior is confirmed, without plan markers, and tersely; the branch's comments and test summaries were cut to that.

The fourteenth round's last code findings (false results at M3-L's and M2-M's limit, false verdicts from the global attempt, the M4-M rule's loss at a crisp placement, the rule's untested edges at M2-M, M3-M one short and M4-Q) were verified only in part and are not fixed on this branch: they share one cause and go to phase 3c.

### Phase 3b: the failure path does the same work once (2026-09-24)

**Done.** Measured first, on `main` at 76ac1cb: the sweep and the corpus as the baseline, and a failing decode profiled on 512 × 512 noise-like images. rMQR spent 87 % of it in `TryLocateSubFinder`; Micro QR spent 90 % in the scale and perspective searches, of which sampling the grid 32 %, codeword extraction 27 %, Reed-Solomon 13 %, the format read 9 % and the transpose for the mirrored read 5 %. Three changes, each a structure rather than a tuning, each bit-identical in result:

- **rMQR: the sub-finder search screens its positions on the lattice they share.** A position (ou, ov) samples the half-module lattice points (ou + 2i, ov + 2j), so each point served up to 25 positions and was read once per position. The lattice is now classified once per lean (dark, light, outside, or next to a pixel edge), and a position with two certain mismatches, which cannot reach the 24 of 25 the template needs, is dropped with all the positions of its row at once in 64-bit masks; the first three rings are scored as before, since a sub-finder near its prediction is found before the screen pays for itself. The screen's float expression is not the search's, so a point within a margin of a pixel edge or the border counts as a match either way; the margin is 2^-18 of the largest coordinate either expression forms, several times their rounding. With it, the ring walk no longer visits each ring's interior (it had been O(r³) iterations, invisible while scoring dominated), and the classification has a 128-bit tier and a branch-free pixel read (on texture the class is a coin toss, and the branch mispredicted half the time: that alone was 1.5x).
- **Micro QR: each grid is sampled once and read in both orientations.** The mirrored read goes through a view that transposes the module index instead of a transposed copy, codewords come through a placement table per size (built from the encoder's own function-module and mask predicates) instead of testing both per module, the affine sampler has a 128-bit tier (lane for lane the scalar arithmetic, as the Standard QR sampler's), and the format word is looked up in a 32 KB table of the words within 3 bits of each candidate (the balls never overlap, so the table is the search).
- **Both: the full sweep does not decode a candidate the strided scan already decoded.** A candidate's decode reads its position and module size, the image, the threshold and, for Micro QR, the refusal rule, and the scan's best failure only once that is terminal; so a candidate the strided scan tried without settling settles on nothing in the sweep either, and skipping it changes only a failure the caller does not report. The sweep still tries its own first eight: a skipped one is not replaced by the ninth. When the strided scan settled (a verdict on the content), nothing is skipped. The plan expected 10-20 % here; on Micro QR it was most of the lever, because the sweep's top eight on texture are largely the strided scan's.

Tests: `RmQRSubFinderScreenParityTest` (56 a framework): the search against the unscreened search transcribed, bit for bit over 1,440 frames (turned and keystoned symbols, symbols framed off, symbols 10,000 to 20,000 px out on a wide image, two-level and 8-bit noise); every dropped position scoring under the floor when sampled the search's way; every certain lattice point the pixel the search reads, on a one-pixel checkerboard 16,000 to 32,768 px out (setting the margin to 0 fails it; dropping at one mismatch instead of two fails the drop test); vector against scalar classification; most positions dropped on noise; a lattice wider than a mask refused. `MicroQRGridReadParityTest` (49 a framework): the matrix decode against the whole-matrix extraction transcribed, over real, damaged and random-under-a-valid-format matrices (status, diagnostics, characters written and the whole destination); the transposed view against a transposed copy (a view that does not swap fails 16); vector against scalar affine sampling, frames off the image included; the format table against the search for all 65,536 words. `ContentVerdictImageDecodeTest.TwoUnmappedSymbols_TheSweepReportsTheOneTheStridedScanFound` (Micro QR, rMQR) holds the skip's one exception: a strided scan that settled on a verdict leaves the sweep every candidate, and without that the sweep reports a second unmapped symbol only it finds instead of the first; it passes on `main` and fails with the exception removed.

**Exit met.** Sweep and corpus against `main`, image for image: Standard QR 54,400, Micro QR 18,400, rMQR 29,440 and the 624 corpus reads identical in every FeatherQR status (the result files differ in one line, zxing-cpp's verdict on one Standard QR render, a code path this phase does not touch). Full suite green on both frameworks (28,057; 378 skipped on this x64 machine). Failing decodes, fastest of interleaved runs, `main` → this phase, ms:

| Image (512 × 512 unless named) | Micro QR | rMQR | Standard QR |
|---|---|---|---|
| two-level noise, 1 px | 91.7 → 47.6 (1.9x) | 119 → 16.8 (7.1x) | 0.94 → 0.91 |
| two-level noise, 2 px | 60.3 → 25.7 (2.3x) | 56.1 → 7.7 (7.3x) | 0.71 → 0.69 |
| 8-bit noise | 200 → 108 (1.9x) | 253 → 40.1 (6.3x) | 2.46 → 2.40 |
| ramp ±20 noise, 256 px | 28.3 → 10.9 (2.6x) | 18.2 → 2.76 (6.6x) | 0.22 → 0.21 |
| ramp ±20 noise | 36.4 → 17.4 (2.1x) | 48.3 → 6.69 (7.2x) | 1.36 → 1.37 |
| ramp ±40 noise (textured) | 17.6 → 11.4 (1.5x) | 29.1 → 6.05 (4.8x) | 1.37 → 1.34 |
| blurred noise, plain ramp | unchanged | unchanged | unchanged |
| Standard QR v10 at 4 px | 77.6 → 31.2 (2.5x) | 11.9 → 3.66 (3.3x) | (reads) |
| Micro QR M4, turned / keystoned | (reads) | 2.70 → 1.31, 2.31 → 1.19 | |
| rMQR R13x139 turned / keystoned, R7x43 turned | 30.9 → 14.2, 25.9 → 7.9, 32.6 → 18.5 | (reads) | |

Per lever, on 8-bit noise: the rMQR screen alone 2.0x, with the ring walk 2.6x, the vector classification 3.5x, the branch-free read 5.4x; the Micro QR grid path alone 1.6x (1.5-1.7x over the images above); the sweep skip then took Micro QR to 1.9x and rMQR to 6.3x. The kernels against the end to end: `TryLocateSubFinder` 221 → 17 ms a decode (13x) for 6.3x; Micro QR sampling, extraction, transpose and format 132 → 46 ms (2.9x) for 1.9x.

**Refuted, with the numbers.**

- **Micro QR modules read on demand through the transform**, format first and data in placement order, no grid sampled whole: 1.2x. Both orientations read their data modules (about 190 each), more than the 289 of one whole-grid sample, and each read paid the float work of a sample. Replaced by one vector sample read through a transposing view.
- **Remembering grids already decoded** within a candidate's search: 523 of 197,432 grids on 8-bit noise repeated the one before and 993 any earlier one of the same candidate; 19 % on a Standard QR image. Not built.
- **Stopping Reed-Solomon once the error locator's degree passes Micro QR's capacity**: not bit-identical. A block the capacity cap rejects reports its corrected-error count in the diagnostics, and only the full correction computes it.

**Lessons.** A search over positions that share their samples is a search over the samples: the 2,601 positions of a lean on the widest symbol read the same 3,481 lattice points up to 25 times each, and once the points are read once, scoring every position is a handful of mask operations. The exactness came from a margin, not from reproducing the float expression: two expressions of the same point differ by a few roundings, and a point near a pixel edge simply says nothing. Removing the dominant cost exposed two that had been hidden under it and that no profile of the old code showed: a ring walk that visited every ring's interior, and a pixel branch that texture mispredicts half the time. And a module read on demand is only cheaper when fewer modules are read: two orientations of one grid read more of it than sampling it once, so the cut that worked was the transpose, not the sample.

**Benchmark delta.** Success paths unchanged (ShortRun, fastest of two interleaved rounds, `main` → this phase): `M4_ImageDecode_Span` 5.03 → 4.70 µs, `R7x43_ImageDecode_Span` 5.39 → 5.29 µs, `R17x139_ImageDecode_Span` 20.3 → 19.5 µs, `R7x43_BitmapDecode` 7.56 → 7.68 µs, `R17x139_BitmapDecode` 31.2 → 29.7 µs, allocations unchanged. Failure paths: `NoSymbol_Noise_1144x168` 6,829 → 885 µs (7.7x), `M1_TurnedUnderShadow_ImageDecode_Fails` 912 → 547 µs, `M1_UnderShadow_ImageDecode_Fails` 21.1 → 20.7 µs, `NoSymbol_Gradient_1144x168` 276 → 283 µs (within its error).
